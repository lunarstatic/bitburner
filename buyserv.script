var maxram = 8192;
var homemoney = getServerMoneyAvailable("home");
var servcost25 = getPurchasedServerCost(maxram) * 25;

for (var i = 0; i < getPurchasedServerLimit(); ++i) {

    if (homemoney > servcost25) {
        var hostname = "magi-" + i;
        purchaseServer(hostname, maxram);
    }
}
exec("bl173.js", "home", 1);
